import DefaultTemplate from "../templates/Default";

// Only a placeholder for now
const DailyExercise = () => {
  return (
    <DefaultTemplate title="daily exercise" contentPosition="center">
      <div>DailyExercise</div>
    </DefaultTemplate>
  );
};

export default DailyExercise;
